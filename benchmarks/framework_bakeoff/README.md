# Framework Bake-off Spikes

These examples are isolated and do not rewrite or import framework abstractions into the validated retrieval implementation.

Run each with the framework-specific isolated environment. A missing package must produce an explicit prerequisite error. Compare each framework against `custom_python_example.py` for integration size, dependency count, execution latency, metadata/provenance preservation, and testability.
